{
  "name": "peerflix-gui",
  "version": "0.0.0",
  "description": "Simple GUI for Peerflix",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development electron index.js",
    "startp": "cross-env NODE_ENV=production electron index.js",
    "pack": "build --dir",
    "dist": "electron-builder --publish never",
    "publish": "electron-builder --publish onTag"
  },
  "author": "KristofferK",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^5.1.3",
    "electron": "^1.8.2",
    "electron-builder": "^19.56.0"
  },
  "build": {
    "appId": "my.id",
    "mac": {
      "category": "your.app.category.type"
    }
  },
  "dependencies": {
    "bootstrap": "^4.0.0",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.1"
  }
}
